# Basic Language Features we need

1. float/double support (`1.1f` is a float, `1.3` is a double) (exponential & scientific tokens too?)
2. long/short support (`1h` (is a short), and `1l` is a long, `1b` is a byte)