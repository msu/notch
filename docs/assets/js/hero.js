// The hero is now fully static — no JS-driven animation. This file is kept
// as a placeholder so the script tag in index.html doesn't 404, and so
// future motion can be re-added in one place.
export function init() {}
init();
